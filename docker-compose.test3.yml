# test3: HTTP only behind an external TLS terminator (Cloudflare, Traefik, ...).
# Bind to 8080 so the external proxy owns 80/443.
services:
  web:
    ports:
      - "8080:80"
    volumes:
      - ./nginx/test/http.conf:/etc/nginx/conf.d/site.conf:ro
