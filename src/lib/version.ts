/**
 * App version. Bump on every change:
 *  - small changes / fixes  -> increment the minor part (1.0 -> 1.01 -> 1.02 ...)
 *  - big features / releases -> increment the major part (1.x -> 2.0)
 * Keep in sync with standalone/package.json ("version") and the
 * APP_VERSION constant in standalone/index.html.
 */
export const APP_VERSION = "2.10";
